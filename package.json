{
  "name": "opencode-devkit",
  "version": "0.1.0",
  "private": true,
  "description": "Client-side tools for auditing, analyzing, and optimizing OpenCode configuration files",
  "scripts": {
    "dev": "pnpm -C frontend run dev",
    "build": "pnpm -C frontend run build",
    "lint": "pnpm -C frontend run lint",
    "typecheck": "pnpm -C frontend run typecheck",
    "preview": "pnpm -C frontend run preview"
  },
  "packageManager": "pnpm@11.5.0"
}
